# AGENTS

> 本文件是当前项目的 Codex 协作规则。根目录 `E:\Workspace\AGENTS.md` 的规则优先级更高。

## 项目规则

1. 必须遵守 `E:\Workspace\AGENTS.md`。
2. 所有回复使用中文。
3. 面向零基础用户解释。
4. 开发前必须先看 `PROJECT_PLAN.md` 和 `TASK.md`。
5. 修改代码后必须更新 `CHANGELOG.md`。
6. 发现踩坑必须记录到 `E:\Workspace\01_全局复利踩坑日志.md`。

## 当前项目重点

1. 先梳理已有项目结构。
2. 先确认项目能不能运行。
3. 再决定是否继续开发功能。
4. 不直接大改前端、后端或 Tauri 代码。
5. 不把新文件创建到 Workspace 外。

## Git 规则

1. 每完成一个小步骤，就提交一次 Git。
2. 提交前先检查 `git status`。
3. 缓存、构建产物、日志和密钥不能提交。
4. 如果绑定了 GitHub 仓库，重要节点要推送到 GitHub。
